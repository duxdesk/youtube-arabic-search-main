# Example environment variables (placeholders only)
# Add only non-secret variable names here. Put secrets in local .env which is ignored.

# EXAMPLES:
# VITE_API_URL=https://api.example.com
# VITE_PUBLIC_KEY=your_public_key_here

# Note: Do NOT put secret/service keys here. Keep them in .env and .gitignore .env.
